# 实验记录：中文情绪分类基础任务

## 1. 实验设置

- 时间：2026-05-30 10:36 开始训练，约 10:58 训练完成。
- 环境：`conda` 环境 `ai_girl`，PyTorch `2.3.1+cu121`。
- 设备：NVIDIA GeForce RTX 3060 Laptop GPU，6GB 显存。
- 数据集：SMP2020-EWECT，六类情绪 `neutral/happy/angry/sad/fear/surprise`。
- 模型：`hfl/chinese-roberta-wwm-ext`。
- 训练参数：`batch_size=8`，`epochs=3`，`max_length=128`，`learning_rate=2e-5`，`weight_decay=0.01`。

数据规模：

| split | 样本数 |
| --- | ---: |
| train | 36,322 |
| dev | 3,993 |
| test | 7,985 |

## 2. 运行命令

```bash
cd /home/fr/FR/AI_Gril/work1/emotion_cls

conda run -n ai_girl python train.py \
  --batch_size 8 \
  --epochs 3 \
  --output_dir outputs/roberta_wwm_ext

conda run -n ai_girl python evaluate.py \
  --model_dir outputs/roberta_wwm_ext \
  --output_dir results/roberta_wwm_ext
```

路线 B 流式 demo：

```bash
conda run -n ai_girl python innovation/streaming_emotion.py \
  "我今天本来很开心，但是后来真的有点难过" \
  --model_dir outputs/roberta_wwm_ext \
  --window_size 6 \
  --lambda_weight 0.7 \
  --memory_decay 0.75 \
  --table
```

## 3. 训练结果

验证集结果：

| epoch | train loss | dev accuracy | dev macro-F1 | dev loss |
| ---: | ---: | ---: | ---: | ---: |
| 1 | 0.7308 | 0.7856 | 0.7238 | 0.6045 |
| 2 | 0.4277 | 0.7899 | 0.7255 | 0.6223 |
| 3 | 0.2379 | 0.7876 | 0.7254 | 0.7192 |

最佳 checkpoint 来自 epoch 2，保存于：

```text
outputs/roberta_wwm_ext/
```

模型目录大小约 `391M`。

## 4. 测试集结果

整体指标：

| metric | value |
| --- | ---: |
| accuracy | 0.7904 |
| macro-F1 | 0.7405 |
| samples | 7,985 |

分类报告：

| 类别 | precision | recall | F1 | support |
| --- | ---: | ---: | ---: | ---: |
| 中性 | 0.79 | 0.78 | 0.78 | 1504 |
| 开心 | 0.86 | 0.88 | 0.87 | 2558 |
| 愤怒 | 0.83 | 0.80 | 0.81 | 1962 |
| 悲伤 | 0.67 | 0.71 | 0.69 | 1119 |
| 恐惧 | 0.68 | 0.66 | 0.67 | 400 |
| 惊讶 | 0.63 | 0.60 | 0.62 | 442 |

测试结果文件：

```text
results/roberta_wwm_ext/metrics.json
results/roberta_wwm_ext/classification_report.txt
results/roberta_wwm_ext/confusion_matrix.png
```

## 5. 单句预测样例

| 输入 | 预测情绪 | 置信度 | 数字人状态 |
| --- | --- | ---: | --- |
| 我今天真的很开心 | happy / 开心 | 0.9975 | smile, cheer, bright |
| 我有点担心明天会不会出问题 | fear / 恐惧 | 0.8363 | worried, reassure, gentle |
| 这件事真的气死我了 | angry / 愤怒 | 0.9942 | serious, restrain, steady |
| 如果失败了我会很难过 | sad / 悲伤 | 0.9855 | sad, comfort, soft |

## 6. 路线 B 流式 demo 观察

输入：

```text
我今天本来很开心，但是后来真的有点难过
```

关键 token 状态：

| token index | token | window | emotion | confidence |
| ---: | --- | --- | --- | ---: |
| 7 | 心 | 天本来很开心 | happy | 0.7026 |
| 10 | 是 | 很开心，但是 | happy | 0.8183 |
| 12 | 来 | 心，但是后来 | sad | 0.7080 |
| 14 | 的 | 但是后来真的 | sad | 0.7834 |
| 18 | 过 | 真的有点难过 | sad | 0.8780 |

现象：模型在前半句逐步转向 `happy`，在“但是后来”之后开始转向 `sad`，到“难过”处悲伤置信度最高。这个结果符合路线 B 的目标：数字人可以随着文本流推进逐步调整情绪，而不是等整句结束后才切换。

完整流式结果保存于：

```text
results/streaming_demo_20260530_103611.json
```

## 7. 简要结论

本次基础任务训练成功，测试集 accuracy 约 `79.04%`，macro-F1 约 `74.05%`。模型对开心、愤怒、中性识别较好，对惊讶、恐惧等少样本类别相对较弱。该模型已经可以作为路线 B 流式 token 情绪状态估计的窗口级情绪分类器。
