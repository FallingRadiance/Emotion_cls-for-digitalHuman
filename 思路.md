# 实时 token 级情绪识别思路

## 1. 核心公式

```text
p_t = f(p_{t-k:t-1}, x_{t-k:t})
```

含义：

- `x_{t-k:t}`：当前 token 以及前面一段 token 构成的文本窗口。
- `p_{t-k:t-1}`：最近 `k` 个 token 的历史情绪概率序列。
- `p_t`：当前 token 的情绪概率分布。

这个公式表示：当前 token 的情绪状态不只由当前文本决定，还要参考最近一段情绪演化轨迹，适合数字人做流式、连续的情绪变化。

## 2. 序列建模形式

```text
h_t = Encoder(x_{t-k:t}, p_{t-k:t-1})
p_t = Softmax(W h_t + b)
```

含义：

- `Encoder` 同时编码文本窗口和历史情绪序列。
- `h_t` 是融合文本语义和情绪记忆后的隐藏表示。
- `Softmax(W h_t + b)` 输出当前 token 属于各情绪类别的概率。

这相当于把实时情绪识别建模为一个带情绪记忆的序列预测问题。

## 3. 两条实现路线

### 路线 A：伪标签 + 序列模型

先用句子级 BERT 情绪分类器对滑动窗口生成 token/片段级伪标签，再训练一个轻量序列模型：

```text
token window + history emotion -> GRU/LSTM/Transformer -> current emotion
```

优点：最贴近公式，有独立模型创新。

缺点：没有真实 token 级人工标注，伪标签质量会影响训练效果。

### 路线 B：不额外训练，递推推理

先用 BERT 对当前窗口预测文本情绪证据：

```text
q_t = BERT(x_{t-k:t})
```

再融合历史情绪记忆：

```text
m_t = weighted_average(p_{t-k:t-1})
p_t = λ q_t + (1 - λ) m_t
```
q_t：即时情绪分布
p_t：平滑后的情绪状态分布

优点：实现简单，不需要 token 级标注，适合作业落地。

缺点：创新主要在推理机制，不是训练新模型。
